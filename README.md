# OC - The OpenGeoDB Data Converter.

This app is converting the CSV-files from OpenGeoDB to JSON-files, with
each Line in a separated JSON-object.

## Build

    rebar3 escriptize

## Build with docker

    docker build .
